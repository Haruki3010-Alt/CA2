<!DOCTYPE html>
<html>
<head>
    <title>All Rental Records</title>
</head>
<body>
    <h1>All Rental Records</h1>

    <table border="1" cellpadding="10">
        <tr>
            <th>User</th>
            <th>Car</th>
            <th>Quantity</th>
            <th>Date</th>
        </tr>
        <% history.forEach(entry => { %>
            <tr>
                <td><%= entry.user %></td>
                <td><%= entry.car %></td>
                <td><%= entry.quantity %></td>
                <td><%= new Date(entry.date).toLocaleString() %></td>
            </tr>
        <% }) %>
    </table>

    <a href="/carInventory">Back to Admin Panel</a>
</body>
</html>
